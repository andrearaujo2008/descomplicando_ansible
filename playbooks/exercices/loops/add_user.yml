---
  - name: Add several users
    hosts: rocky
    become: yes
    tasks:
    - name: Adding several users
      ansible.builtin.user:
        name: "{{ item }}"
        state: present
        groups: "wheel"
      loop:
         - testuser1
         - testuser2
...
